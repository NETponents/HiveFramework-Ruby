def declare_warning(error, isInteractive)
  if isInteractive
    puts "An error has occured, continuing operation."
    puts error
  else
    puts "An error has occured, continuing operation."
    puts error
  end
end
